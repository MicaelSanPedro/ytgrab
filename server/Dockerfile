# Servidor do YTGrab — instância pessoal do Cobalt (open source, AGPL)
# https://github.com/imputnet/cobalt
#
# A imagem já vem pronta (API apenas, sem banco de dados).
# Variáveis de ambiente (definidas no painel do Render):
#   API_URL  — URL pública da instância (ex.: https://sua-url.onrender.com/)  [obrigatória]
#   API_PORT — porta interna do container (padrão 9000)
FROM ghcr.io/imputnet/cobalt:latest
